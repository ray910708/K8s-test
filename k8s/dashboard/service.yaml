apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: microservices-demo
  labels:
    app: dashboard
spec:
  type: ClusterIP
  selector:
    app: dashboard
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
